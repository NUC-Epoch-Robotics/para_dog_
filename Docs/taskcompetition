# 任务赛执行流程

## 1. 启动与初始化
1. 机器人在启动区上电，完成传感器、执行器、自检。
2. 读取比赛开始信号，进入自主模式，禁止人工遥控。

## 2. 智力题识别与高分区确定
1. 行进到题目识别位，触发视觉识别题面。
2. 在 20 秒内完成计算并输出答案（屏显或语音播放）。
3. 计算高分区编号：`high_zone = answer % 4`。
4. 若 20 秒超时仍未确定编号，则本轮 `high_zone = NONE`。

## 3. 主循环（直到 8 个箱体全部处理）
1. 扫描并定位所有未归还 box，选择最近且可达目标。
2. 规划到目标 box 的最优路径（避障、时间代价、姿态稳定约束）。
3. 行至目标 box 附近，精调位姿至吸盘可稳定吸附。
4. 吸附并举升 box，确认抓取稳定（防脱落判定通过）。
5. 识别 box 颜色/类别，映射到对应归还区。
6. 规划到目标归还区的可行最优路线及放置方式。
7. 行至归还区（或归还区前缓冲位），精调位姿。
8. 放下 box，完成放置确认。
9. 若归还区已有箱体，采用非粗暴方式将新箱体放入可计分区域更深处。
10. 记录本次结果（正确得分/错误罚分），标记该 box 已处理。

## 4. 异常与恢复
1. 若机器人摔倒或故障，进入故障态并等待裁判允许后人工扶正/修复。
2. 修复后必须空载返回启动区重新启动任务。
3. 修复期间比赛计时不中断。

## 5. 结束条件
1. 当全部 box 均已处理，退出主循环。
2. 机器人返回启动区，任务结束。

## 6. 关键实现约束
1. 全程自主决策：目标类型、搬运顺序、单次搬运数量不限。
2. 禁止踢、踹、撞等粗暴移送方式。
3. 仅放入对应归还区计分，放错区罚分。













